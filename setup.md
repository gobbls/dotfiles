[ ] Connect to wifi using NetworkManager cli `nmcli`
[ ] Enable `multilib` in `/etc/pacman.conf`
[ ] Install dependencies `./pacman-install`
[ ] Change shell to zsh `chsh`
[ ] Install rust with `rustup default stable`
[ ] Install Firefox and log into account, then Proton Pass
[ ] Clone, build and install `paru`
[ ] Change NPM global install dir with `mkdir ~/.npm-global && npm config set prefix '~/.npm-global'`
    This is to avoid having to use `sudo` when installing NPM packages globally.
[ ] Install Neovim's NodeJS dependency with `npm install -g neovim`
